# models 폴더 안내

이 폴더는 학습된 모델 파일을 저장하는 위치입니다.

## 현재 상태

아직 학습된 모델은 없습니다.

## 사용 원칙

- 모델 파일명에는 실험 ID와 날짜를 포함합니다.
- 대용량 모델 파일은 GitHub에 직접 올리지 않습니다.
- 어떤 코드와 설정으로 만들어진 모델인지 `experiments/`에 기록합니다.

예시:

```text
models/
`-- exp01_tfidf_logreg_YYYYMMDD.pkl
```
